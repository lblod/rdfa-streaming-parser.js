steps:
  install:
    image: codeberg.org/woodpecker-plugins/node-pm
    settings:
      run: install
      with: yarn
  release:
    image: plugins/npm
    settings:
      token:
        from_secret: npm_access_token
when:
  event: tag
  ref: refs/tags/v*
